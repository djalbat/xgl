'use strict';

const VERTICES_LENGTH = 3,
      MINIMUM_DISTANCE = 1,
      DEGREES_TO_RADIANS = Math.PI / 180,
      FIELD_OF_VIEW = 45 * DEGREES_TO_RADIANS,
      Z_NEAR = 1,
      Z_FAR = 1000,
      MOUSE_UP = 'MOUSE_UP',
      MOUSE_DOWN = 'MOUSE_DOWN',
      MOUSE_MOVE = 'MOUSE_MOVE',
      MOUSE_WHEEL = 'MOUSE_WHEEL',
      CTRL_KEY_CODE = 17,
      SHIFT_KEY_CODE = 16,
      OFFSET_SCALAR = 0.25,
      DISTANCE_SCALAR = 1.25,
      ANGLE_COORDINATES_SCALAR = 0.01,
      INITIAL_MOUSE_COORDINATES = [ 0, 0 ],
      INITIAL_ANGLE_COORDINATES = [ 0, 0 ],
      DEFAULT_MARGIN_OF_ERROR = 0.0000001;

module.exports = {
  Z_FAR,
  Z_NEAR,
  MOUSE_UP,
  MOUSE_DOWN,
  MOUSE_MOVE,
  MOUSE_WHEEL,
  CTRL_KEY_CODE,
  SHIFT_KEY_CODE,
  FIELD_OF_VIEW,
  VERTICES_LENGTH,
  MINIMUM_DISTANCE,
  DEGREES_TO_RADIANS,
  OFFSET_SCALAR,
  DISTANCE_SCALAR,
  ANGLE_COORDINATES_SCALAR,
  INITIAL_MOUSE_COORDINATES,
  INITIAL_ANGLE_COORDINATES,
  DEFAULT_MARGIN_OF_ERROR
};
